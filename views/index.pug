extends layout

block content
  h1= title
  p Welcome to #{title}
  br
  form(action='/', method='POST')
    button(submit="button") Add Message 
  br
  each val in msg
    p= `${val.user}: ${val.text} (${val.added})`